// const express = require('express');
// const cartController = require('../../controllers/cart.controller');
// const { authUser } = require('../../middlewares/auth');

// const router = express.Router();

// // route to add a product/package to cart
// router.route('/').post(authUser(), cartController.addToCart);

// // route to remove a product/package from cart
// router.route('/').delete(authUser(), cartController.removeFromCart);

// // route to get the cart of logged-in user
// router.route('/').get(authUser(), cartController.getCart);

// module.exports = router;
